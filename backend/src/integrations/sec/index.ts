/**
 * SEC Integration
 * 
 * Centralized SEC EDGAR API client and utilities
 */

export { SECClient, secClient, createSECClient, SEC_HEADERS } from './client';
export type { SECClientConfig } from './client';
